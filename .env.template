# Copy this file to .env and fill in the actual values before running docker-compose
# keep this template tracked in git, but never commit the filled .env

MASTER_API_TOKEN=
DB_INFO=
DASHPORT=
HOST=

# JSON list of bot clients; remove secrets before committing
CLIENTS=[]

# any other service-specific variables (e.g. API keys, connection strings)
# EXAMPLE_API_KEY=
# REDIS_URL=

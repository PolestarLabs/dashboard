// Ambient declarations for optional or externally-typed packages
// Placed in a dedicated file so the main source code can remain clean.
// These modules are not guaranteed to be installed in all environments; the
// runtime code guards their usage, so we only provide a very loose typing.

declare module "stackblur-canvas";
declare module "@polestar/skia-twemoji";
declare module "canvas-text-wrapper";
